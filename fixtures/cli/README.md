# CLI codec fixtures

PNG/JPEG structure, ICC assignment, and edge/soften tests generate their own
tiny synthetic files at runtime (RGBA with sRGB/gAMA, plus a 4:4:4 near-lossless
JPEG). They are not checked in.
