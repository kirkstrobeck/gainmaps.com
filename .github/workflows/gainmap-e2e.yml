name: gainmap-e2e
on:
  pull_request:
  workflow_dispatch:
jobs:
  docker-e2e:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Build e2e image
        run: docker build -f packages/gainmap/Dockerfile.e2e .
